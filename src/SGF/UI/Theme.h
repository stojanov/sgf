#pragma once

#include <SFML/Graphics.hpp>

SGF_NAMESPACE(::UI)

static const auto ACTIVE_COLOR = sf::Color::White;
static const auto SHADOW_COLOR = sf::Color::Black;
static const auto FOREGROUND_COLOR = sf::Color::White;
static const auto BACKGROUND_COLOR = sf::Color::Black;

END_NAMESPACE

